const fs = require('fs');
const path = require('path');

// Inside the MySQL configuration block:
dialectOptions: {
  charset: 'utf8mb4',
  ssl: {
    ca: fs.readFileSync(path.join(__dirname, 'aiven-ca.pem'))
  }
}